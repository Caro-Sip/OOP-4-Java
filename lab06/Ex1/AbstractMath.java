package Ex1;

public class AbstractMath {
    int reverseNumber(int num) {
        // override to return a reverse number 
        // 12345 to 54321
        return 0;
    }

    int roundedNumber(double num) {
        // override to return a rounded number
        // 3.14 return 3
        // 3.5 return 4
        // 3.7 return 4
        return 0;
    }

    int countDistinct(int[] values) {
        // override to return the count of unique value
        // 1 1 1 0 1 5 9 -> unique 3
        // 5 -5 1 6 0 0 -> 4 unique
        return 0;
    }

    int decimalToBinary(int decimal) {
        // override to return the binary
        // converted from decimal
        // 27 to 11011
        // 121 to 1111001
        return 0;
    }
}
